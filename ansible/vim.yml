- name: setup vim
  hosts: all

  tasks:

    - name: Init opam
      command: opam init -y

    - name: Install merlin
      command: opam install -y merlin

    - name: Setup merlin
      command: opam user-setup install
