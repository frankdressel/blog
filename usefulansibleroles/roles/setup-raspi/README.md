# Setup raspi

This role creates an access point with device wlan0 (make sure, you have another wlan-adapter at wlan1 if needed) with a given name and password. The configuration file is in vars/main.yml and needs to be adapted.
